{
  "name": "fantasy-football",
  "version": "1.0.0",
  "private": true,
  "description": "Draft-prep web UI over the published league-scored NFL data.",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "tsc --noEmit",
    "test": "node -e \"if (+process.versions.node.split('.')[0] < 22) { console.error('\\n  npm test runs the TypeScript tests through node --test, which needs Node >= 22.\\n  This repo builds on Node 24 in production; .nvmrc pins it. Run: nvm use\\n'); process.exit(1) }\" && node --test lib/*.test.ts"
  },
  "dependencies": {
    "@supabase/ssr": "^0.12.4",
    "@supabase/supabase-js": "^2.109.0",
    "next": "^16.3.1",
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "server-only": "^0.0.1"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "@types/react": "^19.2.18",
    "@types/react-dom": "^19.2.4",
    "typescript": "^7.0.2"
  }
}
